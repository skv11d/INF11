word = input().lower()
length = len(word)

print(length)

is_palindrome = True

for i in range(length // 2):
    if word[i] != word[length - 1 - i]:
        is_palindrome = False
        break  

print("true" if is_palindrome else "false")